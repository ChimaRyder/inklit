<div class="flex flex-col justify-center items-center gap-2 pt-5">
    <x-gmdi-chat class="w-10 h-10 text-gray-400"/>
    <h1 class="text-lg font-bold text-gray-400">No Comments</h1>
@if(!empty($subtitle))
    <p class="text-sm text-gray-400">{{ $subtitle }}</p>
    @endif
    </div>
